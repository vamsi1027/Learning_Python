# TODO : Encapsulation
class ExampleEncapsulation:
    def __int__(self, name, age):
        self.name = name
        self.age = age


response = ExampleEncapsulation('harish', 23)
print(response.name)
print(response.age)
